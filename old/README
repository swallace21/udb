Building DB from scratch:

- assuming "udb" database already exists in postgres.

- psql -h <host> udb
  psql> \i sql/udb.sql
  psql \q

cd to build scripts:

   ./cdb2udb

Which sucks in the cdb data

  index -f cat sun '' | ./indexsun2udb
  index -f cat pc '' | ./indexpc2udb
  index -f cat surplus '' | ./indexsurp2udb

If need to rebuild PyDO file, run
   python2.2 ..../lib/PyDO/pgenscript.py
     connect string:


Debian packages required (in addition to standard ksupdate ones):
========================
 python2.2-egenix-mxdatetime
 python-pygresql
