#!/usr/bin/perl

use Getopt::Long;
use Pod::Usage;

use DBI qw(:sql_types);
use DBD::Pg qw(:pg_types);
use Data::Dumper;

eval('require "/tstaff/share/cdb/db.pl";');

use FindBin qw($RealBin);
use lib "$RealBin/../lib";
use BrownCS::UDB;
use BrownCS::UDB::Util qw(ask ask_password confirm);

# Print a simple help message.
sub usage {
  my ($exit_status) = @_;
  pod2usage({ -exitval => $exit_status, -verbose => 99, -sections => "SYNOPSIS|DESCRIPTION|OPTIONS"});
}

my $help = 0;
my $username = $ENV{'USER'};
my $udb = BrownCS::UDB->new;

GetOptions ('help|h|?' => \$help,
            'u' => \$username) or usage(2);
usage(1) if $help;

my $password = &ask_password;

$udb->start($username, $password);

#$udb->{dbh}->trace(1);

print "deleting old data... ";

$udb->{dbh}->do("DELETE FROM equipment;");
$udb->{dbh}->do("DELETE FROM computers;");
$udb->{dbh}->do("DELETE FROM net_interfaces;");
$udb->{dbh}->do("DELETE FROM net_addresses;");
$udb->{dbh}->do("DELETE FROM net_addresses_net_interfaces;");
$udb->{dbh}->do("DELETE FROM net_dns_entries;");
$udb->{dbh}->do("DELETE FROM comp_classes;");
$udb->{dbh}->do("DELETE FROM comp_classes_computers;");
$udb->{dbh}->do("DELETE FROM net_zones; DELETE FROM net_vlans;");
$udb->{dbh}->do("DELETE FROM equip_usage_types; DELETE FROM equip_status_types; delete from management_types;");

print "done.\n";

print "populating static tables... ";

sub domain_insert {
  my ($table, $desc) = @_;
  my $sth = $udb->prepare("INSERT INTO $table (name) VALUES (?) RETURNING name");
  $sth->execute($desc);
  return $sth->fetch()->[0];
}

my %usage_types = (
  "tstaff" => domain_insert("equip_usage_types", "tstaff"),
  "academic" => domain_insert("equip_usage_types", "academic"),
  "personal" => domain_insert("equip_usage_types", "personal"),
);

my %status_types = (
  "deployed" => domain_insert("equip_status_types", "deployed"),
  "spare" => domain_insert("equip_status_types", "spare"),
  "surplus" => domain_insert("equip_status_types", "surplus"),
);

my %management_types = (
  "tstaff" => domain_insert("management_types", "tstaff"),
  "cis" => domain_insert("management_types", "cis"),
  "user" => domain_insert("management_types", "user"),
);

my $zone_insert = $udb->{dbh}->prepare("INSERT INTO net_zones (manager, routing, name, dynamic_dhcp) VALUES (?, ?, ?, ?)");
$zone_insert->bind_param(4, undef, {pg_type => PG_BOOL});

my $vlan_insert = $udb->{dbh}->prepare("INSERT INTO net_vlans (zone, vlan_num, network) VALUES (?, ?, ?)");
$vlan_insert->bind_param(2, undef, SQL_INTEGER);
$vlan_insert->bind_param(3, undef, {pg_type => PG_CIDR});

$zone_insert->execute($management_types{"tstaff"}, "DMZ", "tstaff-dmz", 0);
$zone_insert->execute($management_types{"tstaff"}, "private", "tstaff-private", 0);
$zone_insert->execute($management_types{"tstaff"}, "standard", "tstaff-standard", 0);
$zone_insert->execute($management_types{"user"}, "DMZ", "user-dmz", 0);
$zone_insert->execute($management_types{"user"}, "private", "cs166", 0);
$zone_insert->execute($management_types{"user"}, "special", "outside", 0);
$zone_insert->execute($management_types{"user"}, "special", "jj-oshean", 0);
$zone_insert->execute($management_types{"user"}, "special", "techhouse", 0);
$zone_insert->execute($management_types{"user"}, "special", "ipsec", 0);
$zone_insert->execute($management_types{"user"}, "standard", "user-standard", 0);

$vlan_insert->execute("tstaff-dmz", 32, "128.148.32.0/25");
$vlan_insert->execute("tstaff-private", 192, "192.168.1.0/24");
$vlan_insert->execute("tstaff-private", 898, "10.116.0.0/16");
$vlan_insert->execute("tstaff-private", 897, "192.168.10.0/24");
$vlan_insert->execute("tstaff-standard", 31, "128.148.31.0/24");
$vlan_insert->execute("tstaff-standard", 33, "128.148.33.0/24");
$vlan_insert->execute("tstaff-standard", 37, "128.148.37.0/24");
$vlan_insert->execute("tstaff-standard", 38, "128.148.38.0/24");
$vlan_insert->execute("user-dmz", 892, "128.148.32.128/25");
$vlan_insert->execute("cs166", 893, "192.168.100.0/24");
$vlan_insert->execute("outside", 34, "128.148.34.0/24");
$vlan_insert->execute("jj-oshean", 698, "198.7.242.32/28");
$vlan_insert->execute("techhouse", 360, "138.16.60.0/24");
$vlan_insert->execute("user-standard", 36, "128.148.36.0/24");
$vlan_insert->execute("ipsec", 885, "10.117.0.0/16");

print "done.\n";

print "importing cdb... ";

my($key, $data);
while ( ($key, $data) = each(%$cdb_by_hostname) ) {
  if (( $data->{'status'} ne 'disabled' ) and ($data->{'ethernet'} ne '' )) {
    print "adding host " . $data->{hostname} . "\n";
    $udb->insert_host($data);
  }
}

print "done.\n";

$udb->finish;

__END__

=head1 NAME

import-cdb - import from cdb

=head1 SYNOPSIS

import-cdb [-u username]

=head1 DESCRIPTION

imports from cdb

=head1 OPTIONS

=over

=item B<-h>, B<--help>

Print a help message and exit.

=item B<-u>, B<--username>=user

Logs onto the database server as the specified username, instead of as
the current user.

=back

=head1 AUTHORS

Aleks Bromfield.

=head1 SEE ALSO

B<udb>

=cut

